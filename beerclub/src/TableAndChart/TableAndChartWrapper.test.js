import {render, screen } from '@testing-library/react';
import TableAndChartWrapper from "./TableAndChartWrapper";


describe('TableAndChartWrapper', () => {
  describe('BeerClubTable', () => {
    test('renders table', () => {
      expect(true).toBe(false);
    })
  })
  describe('BeerClubChart', () => {
    test('renders table', () => {
      expect(true).toBe(false);
    })
  })

})
